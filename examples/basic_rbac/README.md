# Basic RBAC example

This safe authorizer enforces tenant isolation and permits account deletion only for admins.

```bash
uv run permissiondiff test --config examples/basic_rbac/permissiondiff.yaml
```
